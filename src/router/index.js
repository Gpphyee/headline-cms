import Vue from 'vue'
import VueRouter from 'vue-router'

Vue.use(VueRouter)

const routes = [
  {
    redirect: '',
    path: ''

  }

]

const router = new VueRouter({
  routes
})

export default router
